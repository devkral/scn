important
-rename name to domain, service to serveclass, exposed services as service
-fix multithreading (later)
-finish gui
-expose real services
-client connection timeouts (remove this feature from the locking module)
-move special services to admin
-rename admin to main

future:
rename specialservices to plugin

enhancements:
-callbacks
-tunnels
